# Direnv configuration for Nix flake
# This file enables automatic environment loading when entering the directory

# Use the Nix flake
use flake

# Optional: Show that environment is loaded
echo "🍝 Pasta development environment loaded!"
echo "Run 'menu' to see available commands"
